# Рекомендации по оптимизации NowPlay

## 🎯 Основные направления оптимизации

### 1. Замена библиотек на более легкие

#### ✅ **Заменить `requests` на `urllib`** (встроенный в Python)
**Текущее использование:**
- `ui/app.py` - проверка порта (можно убрать)
- `ui/pages/settings_page.py` - получение списка источников и обновление конфига

**Экономия:** ~5-10 МБ в сборке

#### ✅ **Оптимизировать использование `aiohttp`**
**Проблема:** aiohttp тянет много зависимостей
**Решение:** 
- Использовать только необходимые модули
- Добавить в `excludes` неиспользуемые модули aiohttp

#### ⚠️ **CustomTkinter** - тяжелая, но необходима для GUI
**Альтернативы:**
- Стандартный tkinter (но потеряете современный вид)
- PyQt5/PySide2 (легче customtkinter, но все равно тяжелые)
- Web-based GUI через браузер (уже частично используется)

### 2. Оптимизация сборки PyInstaller

#### Исключить неиспользуемые модули:
- Многие модули в `hiddenimports` могут быть не нужны
- Добавить `excludes` для неиспользуемых модулей

#### Использовать UPX сжатие (уже включено)
#### Использовать `--onefile` режим с оптимизацией

### 3. Архитектурные изменения

#### ✅ **Объединить процессы**
**Текущее:** GUI (main.py) + Server (now.py) в разных процессах
**Оптимизация:** Запускать сервер в отдельном потоке (threading), а не процессе

#### ✅ **Ленивая загрузка модулей**
Загружать тяжелые библиотеки только когда они нужны

### 4. Альтернативные технологии (радикальный вариант)

#### **Вариант A: C#/.NET (Windows Native)**
- ✅ Лучшая интеграция с Windows
- ✅ Меньший размер exe (~10-20 МБ)
- ✅ Быстрее работает
- ❌ Придется переписать все

#### **Вариант B: Go**
- ✅ Очень маленький размер exe (~5-10 МБ)
- ✅ Быстрый запуск
- ✅ Статическая сборка
- ❌ Нужно переписать

#### **Вариант C: Rust**
- ✅ Экстремально маленький размер
- ✅ Максимальная производительность
- ❌ Сложнее в разработке

## 📊 Оценка текущего веса компонентов

1. **Python Runtime:** ~30-40 МБ
2. **customtkinter:** ~15-20 МБ
3. **aiohttp:** ~10-15 МБ
4. **winsdk:** ~5-10 МБ
5. **PIL/Pillow:** ~5-8 МБ
6. **requests:** ~5 МБ (можно заменить)
7. **Остальное:** ~10-15 МБ

**Итого:** ~80-120 МБ (типичный размер для PyInstaller)

## 🚀 Быстрые улучшения (без переписывания)

### ✅ Приоритет 1: Замена requests на urllib
- **Выполнено!** ✅
- Экономия: 5-10 МБ
- Сложность: Низкая
- Время: 30 минут

### ✅ Приоритет 2: Оптимизация PyInstaller excludes
- **Выполнено!** ✅
- Экономия: 10-20 МБ
- Сложность: Низкая
- Время: 1 час

### ✅ Приоритет 3: Threading вместо multiprocessing
- **Выполнено!** ✅
- Экономия: Память, быстрый запуск
- Сложность: Средняя
- Время: 2 часа

### ⏳ Приоритет 4: Ленивая загрузка модулей
- Экономия: Быстрый запуск
- Сложность: Средняя
- Время: 3 часа

## ✅ Что уже сделано:

1. **Заменен `requests` на `urllib`** - убрана внешняя зависимость (5-10 МБ)
2. **Заменен `multiprocessing` на `threading`** - меньше памяти и быстрее запуск
3. **Оптимизирован `build.spec`** - добавлены excludes для неиспользуемых модулей
4. **Убраны лишние импорты** - очистка кода

## 💡 Рекомендация

**Для быстрого результата:**
1. Заменить `requests` на `urllib` 
2. Оптимизировать PyInstaller excludes
3. Использовать threading вместо multiprocessing

**Для максимальной оптимизации:**
Рассмотреть перепись на C#/.NET для Windows-приложения

